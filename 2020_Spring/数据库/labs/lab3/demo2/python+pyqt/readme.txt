C/S架构的基本demo

安装好必要依赖之后，执行
python main.py

打开浏览器，便进入了一个程序主窗口：

点击菜单>连接，输入mysql数据库运行的主机ip地址，一般为本地，即127.0.0.1；输入想要登录的数据库名称（一般连接后使用use <数据库名> 这个名称），用户名，用户密码，点击提交，即可完成登录。

如果成功，主页面的按钮从不可操作变为可操作
如果失败，登录页面提示失败，确认后退出登录界面，主页面的按钮仍不可见