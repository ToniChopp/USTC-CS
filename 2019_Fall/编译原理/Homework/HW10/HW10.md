# HW10
## 5.5
```
foo的类型表达式：
arry(100,CELL)
CELL的类型表达式：
record((a×integer)×(b×integer))
所以foo的类型表达式写作：
arry(100,record((a×integer)×(b×integer)))
bar的类型表达式：
(integer×record((a×integer)×(b×integer)))→pointer(record((a×integer)×(b×integer)))
```
## 5.6
```
P→D;E
D→D;D
D→id : T    {addtype (id.entry,T.type);}
T→list of T1{T.type=list(T1.type);}
T→char      {T.type=char;}
T→integer   {T.type=integer;}
E→(L)       {E.type=list(L.type);}
E→literal   {E.type=char;}
E→num       {E.type=integer;}
E→id        {E.type=lookup(id.entry)};
L→E,L1      {if(E.type==L1.type)
                L.type=L1.type;
             else
                L.type=type_error;}
L→E         {L.type=E.type;}
```